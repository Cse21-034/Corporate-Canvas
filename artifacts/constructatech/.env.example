# Required by Vite (set automatically on Replit; must be set manually on Vercel)
BASE_PATH=/

# Set this to your Render API URL when deploying the frontend to Vercel.
# Leave blank on Replit (the API is served from the same origin at /api).
# Example: https://constructatech-api.onrender.com
VITE_API_URL=
